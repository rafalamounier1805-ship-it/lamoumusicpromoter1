import {analyzeDeepAudio,deterministicInterpretation} from './dsp-engine.js';

const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const HISTORY_KEY='lamou-ranking-history-v2';
let lastStage2=null;
const esc=(v='')=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const val=(v,d=1,s='')=>Number.isFinite(v)?`${v.toFixed(d)}${s}`:'—';
const tone=n=>n>=85?'good':n>=70?'warn':'bad';
const tag=(t,c='')=>`<span class="tag ${c}">${t}</span>`;
const mini=(l,v)=>`<div class="mini-metric"><strong>${v}</strong><span>${l}</span></div>`;
function setHeader(kicker,title){$('#page-kicker').textContent=kicker;$('#page-title').textContent=title;$$('[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===title.toLowerCase().startsWith('analisar')?'analyze':title.toLowerCase().startsWith('metodologia')?'method':false));}
function setHash(v){history.replaceState(null,'',`#/${v}`)}
function confidence(n){return n>=70?tag('Confiança alta','green'):n>=40?tag('Confiança média','yellow'):tag('Confiança baixa','red')}
function energyStrip(a=[]){return `<div class="s2-energy">${a.map((v,i)=>`<i title="Trecho ${i+1}: ${v.toFixed(0)}%" style="--e:${Math.max(6,v)}%"></i>`).join('')}</div>`}
function spectrum(m){return `<div class="s2-spectrum">${[['Graves',m.lowPct],['Médios',m.midPct],['Agudos',m.highPct]].map(([n,v])=>`<div><span>${n}</span><div><i style="width:${Math.max(2,v)}%"></i></div><b>${v.toFixed(1)}%</b></div>`).join('')}</div>`}
function subScores(s){const labels={structure:'Estrutura · proxy',production:'Produção · proxy',mix:'Mixagem',mastering:'Masterização',replay:'Replay · proxy'};return `<div class="bar-group">${Object.entries(s).map(([k,v])=>`<div class="bar-line"><b>${labels[k]}</b><div class="bar"><span style="width:${v}%"></span></div><strong>${v}</strong></div>`).join('')}</div>`}
function getHistory(){try{return JSON.parse(localStorage.getItem(HISTORY_KEY)||'[]')}catch{return[]}}
function saveHistory(a){localStorage.setItem(HISTORY_KEY,JSON.stringify(a))}
function toast(msg){const t=$('#toast');if(!t)return;t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2200)}

function renderAnalyze2(){
 setHash('analyze');setHeader('ETAPA 2 · MOTOR TÉCNICO','Analisar música');
 const view=$('#view');view.innerHTML=`<div class="hero"><div class="hero-grid"><div><div class="tag-row">${tag('ETAPA 2 · DSP LOCAL','green')}${tag('SEM UPLOAD DO ÁUDIO','blue')}${tag('ENGINE v1.2')}</div><h2>Abra o master.<br><span class="gradient-text">O motor lê o sinal em profundidade.</span></h2><p>A Etapa 2 mede o que o próprio arquivo consegue provar: dinâmica, loudness aproximado, pico intersample aproximado, espectro, estéreo, silêncio, BPM, tonalidade e contraste estrutural. O que exige contexto artístico continua sem nota.</p><div class="hero-actions"><button id="s2-pick" class="btn primary">Escolher música</button><button id="s2-method" class="btn ghost">Ver metodologia técnica</button></div></div><div class="card"><div class="card-label">Privacidade</div><div class="metric good">LOCAL</div><div class="metric-sub">O arquivo permanece no dispositivo.</div><div class="s2-wave">${Array.from({length:34},(_,i)=>`<i style="height:${18+(i*29)%70}%"></i>`).join('')}</div></div></div></div><div id="s2-output" class="section"><div class="card upload-zone"><h3>Nenhum áudio selecionado</h3><p class="muted">MP3, WAV, M4A e outros formatos que o navegador consiga decodificar.</p></div></div><div class="grid cards3 section"><div class="card"><h3>O que entra agora</h3><p class="muted small">True peak aproximado, LUFS aproximado, dinâmica, crest, clipping, espectro, estéreo, silêncio, BPM, tonalidade e estrutura energética.</p>${tag('MEDIDO + CALCULADO','green')}</div><div class="card"><h3>TQS com cobertura explícita</h3><p class="muted small">45% da régua pode ser sustentada tecnicamente pelo master. O restante fica sem nota até haver evidência artística.</p>${tag('TQS PARCIAL · 45%','yellow')}</div><div class="card s2-pro"><h3>Sem atalhos</h3><p class="muted small">Tonalidade não é melodia. BPM não é groove. Espectro não é originalidade. Master não separa voz com segurança.</p>${tag('SEM EVIDÊNCIA = SEM NOTA','red')}</div></div>`;
 $('#s2-pick').onclick=()=>$('#audio-input').click();$('#s2-method').onclick=renderMethod2;
}
async function analyzeFile(file){
 const out=$('#s2-output');if(!out)return;out.innerHTML='<div class="card upload-zone"><div class="s2-spinner"></div><h3>Analisando o master…</h3><p class="muted">Medição local de sinal, espectro, ritmo e estrutura.</p></div>';
 try{
  const ac=new (window.AudioContext||window.webkitAudioContext)(),audio=await ac.decodeAudioData(await file.arrayBuffer()),m=await analyzeDeepAudio(audio);await ac.close();
  lastStage2={id:`CMP-${Date.now()}`,type:'comparacao',date:new Date().toISOString(),title:file.name,score:m.partialTqs,method:'LAMOU Ranking Engine v1.2',coverage:`TQS parcial ${m.coveragePct}%`,metrics:m};
  out.innerHTML=`<div class="section-head"><div><div class="eyebrow">ANÁLISE TÉCNICA LOCAL</div><h3>${esc(file.name)}</h3><p>${val(m.duration,1,' s')} · ${m.channels} canal${m.channels===1?'':'is'} · ${(m.sampleRate/1000).toFixed(1)} kHz</p></div><div class="tag-row">${tag('E4 · ARQUIVO ANALISADO','green')}${tag('v1.2','blue')}</div></div><div class="s2-summary"><div class="card"><div class="card-label">TQS técnico parcial</div><div class="s2-big ${tone(m.partialTqs)}">${m.partialTqs}</div><div class="s2-cover"><i style="width:${m.coveragePct}%"></i></div><p><b>Cobertura ${m.coveragePct}%</b></p><p class="muted small">Normalizado somente sobre critérios sustentados pelo master. Não é TQS completo.</p>${tag('CALCULADO','blue')}</div><div class="card"><div class="card-label">Signal Readiness</div><div class="metric ${tone(m.signalReadiness)}">${m.signalReadiness}</div><p class="muted small">Prontidão técnica do sinal; não mede valor artístico.</p><div class="result-grid s2-mini">${mini('Pico',val(m.peakDb,2,' dBFS'))}${mini('True peak ≈',val(m.truePeakApproxDb,2,' dBTP'))}${mini('LUFS ≈',val(m.lufsApprox,1,' LUFS'))}${mini('Clipping',val(m.clippingPct,4,'%'))}</div></div><div class="card"><div class="card-label">Ritmo e tonalidade · estimativas</div><div class="metric">${m.bpm?`${Math.round(m.bpm)} BPM`:'—'}</div><div class="tag-row">${confidence(m.bpmConfidence)}${tag(`Tonalidade: ${esc(m.key?.label||'—')}`,'blue')}</div><p class="muted small">BPM e tonalidade são inferências DSP. Confiança tonal: ${Math.round(m.key?.confidence||0)}%.</p><div class="result-grid s2-mini">${mini('Tempo estável',m.tempoStability==null?'—':`${Math.round(m.tempoStability)}%`)}${mini('Contraste',val(m.structureContrastDb,1,' dB'))}${mini('Mudanças',m.sectionChanges)}${mini('Silêncio inicial',val(m.headSilenceSec,2,' s'))}</div></div></div><div class="grid cards4 section s2-pro">${metricCard('RMS',val(m.rmsDb,1,' dBFS'),'MEDIDO')}${metricCard('Crest factor',val(m.crestDb,1,' dB'),'CALCULADO')}${metricCard('Dinâmica',val(m.dynamicDb,1,' dB'),'CALCULADO')}${metricCard('Short-term range',val(m.shortTermRangeDb,1,' dB'),'CALCULADO')}</div><div class="split section s2-pro"><div class="card"><div class="section-head"><div><h3>Equilíbrio espectral</h3><p>Energia relativa do master por banda.</p></div>${tag('MEDIDO','green')}</div>${spectrum(m)}<div class="result-grid s2-mini section">${mini('Centroide',val(m.centroidHz,0,' Hz'))}${mini('Rolloff 85%',val(m.rolloff85Hz,0,' Hz'))}${mini('Flatness',val(m.flatness,3))}${mini('Brilho proxy',val(m.brightness,0,'%'))}</div></div><div class="card"><div class="section-head"><div><h3>Imagem estéreo</h3><p>Correlação e energia lateral.</p></div>${m.stereoAvailable?tag('MEDIDO','green'):tag('MONO','yellow')}</div>${m.stereoAvailable?`<div class="s2-stereo"><i style="left:${((m.stereoCorrelation+1)/2*100).toFixed(1)}%"></i></div><div class="result-grid s2-mini">${mini('Correlação',val(m.stereoCorrelation,2))}${mini('Largura proxy',val(m.stereoWidth,0,'%'))}${mini('Side/Mid',val(m.sideRatioDb,1,' dB'))}${mini('DC offset',val(m.dcOffset,5))}</div>`:'<div class="notice warn">Arquivo mono: largura e correlação estéreo não se aplicam.</div>'}</div></div><div class="card section s2-pro"><div class="section-head"><div><h3>Mapa de energia</h3><p>12 blocos de duração equivalente; não presume verso/refrão.</p></div>${tag('ESTRUTURA · PROXY','yellow')}</div>${energyStrip(m.energySegments)}<div class="result-grid s2-mini section">${mini('Contraste',val(m.structureContrastDb,1,' dB'))}${mini('Novidade média',val(m.structureNoveltyDb,1,' dB'))}${mini('Silêncio inicial',val(m.headSilenceSec,2,' s'))}${mini('Silêncio final',val(m.tailSilenceSec,2,' s'))}</div></div><div class="split section"><div class="card s2-pro"><div class="section-head"><div><h3>Critérios TQS observáveis</h3><p>Subnotas transparentes; proxies identificados.</p></div>${tag('COBERTURA 45%','yellow')}</div>${subScores(m.subscores)}</div><div class="card s2-expand"><div class="section-head"><div><h3>Leitura automática do sinal</h3><p>Sem inventar conteúdo artístico.</p></div>${tag('CALCULADO','blue')}</div><p>${esc(deterministicInterpretation(m))}</p><div id="s2-ai" class="notice section">IA opcional: verificando disponibilidade…</div></div></div><div class="grid cards3 section"><div class="card"><h3>Composição + coerência · 12%</h3><div class="metric">—</div><p class="muted small">O master sozinho não prova qualidade composicional.</p>${tag('NÃO DISPONÍVEL','red')}</div><div class="card"><h3>Melodia + hook · 13%</h3><div class="metric">—</div><p class="muted small">Tonalidade detectada não equivale a avaliar melodia ou hook.</p>${tag('NÃO DISPONÍVEL','red')}</div><div class="card"><h3>Performance vocal · 10%</h3><div class="metric">—</div><p class="muted small">Sem stem vocal confiável, não pontuamos afinação/interpretação isolada.</p>${tag('NÃO DISPONÍVEL','red')}</div></div><div class="notice warn section"><b>AIS e CPS permanecem sem nota nesta análise local.</b> Identidade exige contexto de catálogo; potencial comercial exige benchmark e mercado.</div><div class="hero-actions"><button id="s2-save" class="btn primary">Salvar comparação</button><button id="s2-export" class="btn secondary">Exportar JSON técnico</button><button id="s2-pro-btn" class="btn ghost">Ver modo Profissional</button></div>`;
  $('#s2-save').onclick=saveStage2;$('#s2-export').onclick=exportStage2;$('#s2-pro-btn').onclick=()=>{const pro=$$('[data-mode="pro"]')[0];if(pro)pro.click()};requestAI(m);
 }catch(e){out.innerHTML=`<div class="notice red"><b>Não foi possível analisar este arquivo.</b><br>${esc(e.message||String(e))}</div>`}
}
function metricCard(l,v,e){return `<div class="card"><div class="card-label">${l}</div><div class="metric">${v}</div>${tag(e,e==='MEDIDO'?'green':'blue')}</div>`}
function saveStage2(){if(!lastStage2)return;const h=getHistory();h.unshift(lastStage2);saveHistory(h);toast('Comparação técnica salva no Histórico')}
function exportStage2(){if(!lastStage2)return;const blob=new Blob([JSON.stringify(lastStage2,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`lamou-etapa2-${Date.now()}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
async function requestAI(m){const el=$('#s2-ai');if(!el)return;try{const r=await fetch('/api/ai/interpret',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({metrics:{peakDb:m.peakDb,truePeakApproxDb:m.truePeakApproxDb,rmsDb:m.rmsDb,lufsApprox:m.lufsApprox,crestDb:m.crestDb,dynamicDb:m.dynamicDb,clippingPct:m.clippingPct,stereoCorrelation:m.stereoCorrelation,stereoWidth:m.stereoWidth,centroidHz:m.centroidHz,lowPct:m.lowPct,midPct:m.midPct,highPct:m.highPct,bpm:m.bpm,bpmConfidence:m.bpmConfidence,structureContrastDb:m.structureContrastDb}})});if(!r.ok)throw Error();const j=await r.json(),t=j?.result?.response||j?.result?.result||j?.result;if(!t)throw Error();el.className='notice green section';el.innerHTML=`<b>Segunda leitura opcional da IA</b><br>${esc(typeof t==='string'?t:JSON.stringify(t))}`}catch{el.className='notice section';el.innerHTML='<b>IA opcional indisponível nesta execução.</b><br>A análise objetiva acima continua válida dentro da cobertura declarada.'}}
function renderMethod2(){
 setHash('method');setHeader('ETAPA 2 · TRANSPARÊNCIA','Metodologia técnica');const view=$('#view');
 const weights=[['Composição e coerência',12,'não observável só pelo master'],['Melodia e hook',13,'não observável só pelo master'],['Estrutura',10,'proxy energético'],['Arranjo',10,'não pontuado sem contexto'],['Performance vocal',10,'não pontuada sem stem/evidência'],['Produção e sound design',10,'proxy espectral/técnico'],['Mixagem',12,'sinal, espectro e estéreo'],['Masterização',8,'pico, loudness, dinâmica e clipping'],['Originalidade/identidade',10,'não observável em uma faixa isolada'],['Eficiência de replay',5,'proxy de duração/forma']];
 view.innerHTML=`<div class="hero"><div class="hero-grid"><div>${tag('ENGINE v1.2','green')}<h2>Nota só onde existe<br><span class="gradient-text">evidência defensável.</span></h2><p>A Etapa 2 não tenta transformar DSP em opinião artística. Ela amplia a medição e calcula um TQS técnico parcial com cobertura conhecida.</p></div><div class="card"><div class="card-label">Cobertura atual</div><div class="metric warn">45%</div><p class="muted small">Estrutura-proxy, produção-proxy, mix, master e replay-proxy. Os 55% restantes ficam sem nota.</p></div></div></div><div class="evidence-grid section"><div class="evidence-item">${tag('MEDIDO','green')}<p class="muted small">Vem diretamente das amostras do áudio.</p></div><div class="evidence-item">${tag('CALCULADO','blue')}<p class="muted small">Fórmula ou algoritmo declarado.</p></div><div class="evidence-item">${tag('ESTIMADO','yellow')}<p class="muted small">BPM, tonalidade, LUFS e true peak aproximados possuem incerteza explícita.</p></div><div class="evidence-item">${tag('INTERPRETADO','yellow')}<p class="muted small">Leitura textual separada da medição.</p></div><div class="evidence-item">${tag('NÃO DISPONÍVEL','red')}<p class="muted small">Sem evidência suficiente: não pontuar.</p></div></div><div class="split section"><div class="card"><h3>Régua TQS v1.2</h3><table class="method-table"><thead><tr><th>Critério</th><th>Peso</th><th>Etapa 2</th></tr></thead><tbody>${weights.map(x=>`<tr><td>${x[0]}</td><td>${x[1]}%</td><td>${x[2]}</td></tr>`).join('')}</tbody></table></div><div class="card"><h3>Métricas técnicas</h3><p><b>Pico/RMS/Crest:</b> amplitude e densidade.</p><p><b>True peak ≈:</b> aproximação intersample.</p><p><b>LUFS ≈:</b> aproximação inspirada em BS.1770.</p><p><b>Dinâmica:</b> percentis de blocos ativos.</p><p><b>Espectro:</b> FFT em múltiplos trechos.</p><p><b>Estéreo:</b> correlação L/R e relação side/mid.</p><p><b>BPM/tonalidade:</b> estimativas com confiança própria.</p></div></div><div class="card section"><h3>O que o motor recusa inferir</h3><div class="grid cards3"><div><b>Melodia/hook</b><p class="muted small">Tonalidade não mede memorabilidade.</p></div><div><b>Performance vocal</b><p class="muted small">Master completo não é stem vocal.</p></div><div><b>Originalidade</b><p class="muted small">Exige comparação/contexto de catálogo.</p></div><div><b>AIS</b><p class="muted small">Exige identidade ao longo do catálogo.</p></div><div><b>CPS</b><p class="muted small">Exige benchmark e mercado.</p></div><div><b>Origem de IA</b><p class="muted small">Não é inferida por “soar como IA”.</p></div></div></div>`;
}
function intercept(e){
 const mode=e.target.closest?.('[data-mode]');if(mode){setTimeout(()=>{const h=location.hash;if(h==='#/analyze')renderAnalyze2();if(h==='#/method')renderMethod2()},0);return}
 const target=e.target.closest?.('[data-view],#global-compare');if(!target)return;const v=target.id==='global-compare'?'analyze':target.dataset.view;if(v==='method'&&window.__LAMOU_STAGE3__)return;if(v==='analyze'||v==='method'){e.preventDefault();e.stopImmediatePropagation();v==='analyze'?renderAnalyze2():renderMethod2()}
}
document.addEventListener('click',intercept,true);
window.addEventListener('hashchange',()=>setTimeout(()=>{if(location.hash==='#/analyze')renderAnalyze2();if(location.hash==='#/method')renderMethod2()},0));
const input=$('#audio-input');if(input)input.onchange=e=>{const f=e.target.files?.[0];if(f)analyzeFile(f);e.target.value=''};
const engine=$('.engine-tag');if(engine)engine.textContent='Engine v1.2 · Etapa 2';
if(location.hash==='#/analyze')renderAnalyze2();else if(location.hash==='#/method')renderMethod2();